version: '3'
services:
  nodejs:
    image: hiepph1707/nodejs-project:dev
    container_name: nodejs-app
    ports:
      - 3000:3000
    restart: unless-stopped
    volumes:
      - /etc/localtime:/etc/localtime:ro
